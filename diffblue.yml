buildCmd: mvn -e -X -Dmaven.test.skip=true compile
testCmd: true
lineLength: 4096
skipVerification: true
cbmcArguments:
  # Do not generate coverage comments on generated tests.
  java-generate-no-comments: true
phases:
  -
  #Phase 0 PRETTY AND SIMPLE - Simple models, quick cover, pretty Tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # Instrument the file as opposed to the function only
      cover-only: file
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 1 PRETTY AND COMPLEX - Standard models, longer run time, pretty tests, Do Not Test Private Method Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 2
      # Do not test any methods that have the specified access level.
      do-not-test-methods-with-access: [ private ]
      # Instrument the entry point function only.
      cover-function-only: false
      # instrument the file as opposed to the function only
      cover-only: file
  -
  #Phase 2 Simple and Private Directly
    timeout: 180
    cbmcArguments:
      # Use simple models containers (ArrayList, HashMap, HashSet,...) will have a maximum size of one.
      classpath: '/tools/cbmc/models-simple-overlay.jar:/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 1500
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: true
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 10
      # Bound the length of input strings.
      max-nondet-string-length: 10
      # Add constraint that strings are printable
      string-printable: true
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 1
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
      # If the logs contain output of the following then proceed to phase X
    nextPhase:
      # If the function is not analysable then do not pass onto the next phase
      not_analyzed: null
  -
  #Phase 3 Complex and Private Directly
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: 3000
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 3
      # Set string input value  
      string-input-value: ["foo", "Bar", "BAZ", "1234", "a,b,c", "1", "2", "3", "a/b/c", "a'b'c"]
  -
  #Phase 4 EXCEPTIONS
    timeout: 180
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 30
      # Bound the length of input strings.
      max-nondet-string-length: 30
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: true
      # Set an upper bound on loop unwinding.
  -
    #PHASE 5 Big numbers and less contraints on inputs 
    timeout: 300
    cbmcArguments:
      # Use the standard models library.
      classpath: '/tools/cbmc/models.jar:.'
      # Set a limit on the number of instructions that can be generated as part of a test trace.
      depth: false
      # never initialize reference-typed parameter to the entry point with null
      java-assume-inputs-non-null: false
      # Constrain the size of arrays created in the analyzed code.
      java-max-vla-length: 10000
      # Maximum allowed length for an array passed as test input.
      max-nondet-array-length: 100
      # Bound the length of input strings.
      max-nondet-string-length: 100
      # Add constraint that strings are printable
      string-printable: false
      # Make implicit runtime exceptions explicit.
      throw-runtime-exceptions: false
      # Set an upper bound on loop unwinding.
      unwind: 6
units:
# Target TGF: 572.142857143
#  >methods=23
#  >complexity=627
#  >construction=5.14285714286
#  >dependency=12.0
 - target:
   - dir: admin-core/target/classes
     file: com/lrs/core/admin/entity/User.class
     sourceDir: admin-core/src/main/java
     sourceFile: com/lrs/core/admin/entity/User.java
# Target TGF: 1737.0
#  >methods=7
#  >complexity=193
#  >construction=54.0
#  >dependency=6.0
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/Tools.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/Tools.java
# Target TGF: 779.0
#  >methods=34
#  >complexity=857
#  >construction=6.25
#  >dependency=16.0
 - target:
   - dir: admin-core/target/classes
     file: com/lrs/core/admin/entity/Role.class
     sourceDir: admin-core/src/main/java
     sourceFile: com/lrs/core/admin/entity/Role.java
# Target TGF: 779.0
#  >methods=31
#  >complexity=857
#  >construction=6.25
#  >dependency=16.0
 - target:
   - dir: admin-core/target/classes
     file: com/lrs/core/admin/entity/Menu.class
     sourceDir: admin-core/src/main/java
     sourceFile: com/lrs/core/admin/entity/Menu.java
# Target TGF: 1224.0
#  >methods=4
#  >complexity=167
#  >construction=27.0
#  >dependency=2.5
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/ImgUtil.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/ImgUtil.java
# Target TGF: 2974.5
#  >methods=6
#  >complexity=372
#  >construction=36.0
#  >dependency=3.5
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/encrypt/MD5Util.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/encrypt/MD5Util.java
# Target TGF: 5643.0
#  >methods=32
#  >complexity=658
#  >construction=270.0
#  >dependency=29.0
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/date/DateUtil.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/date/DateUtil.java
# Target TGF: 531.0
#  >methods=8
#  >complexity=102
#  >construction=36.0
#  >dependency=2.5
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/encrypt/SHA.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/encrypt/SHA.java
# Target TGF: 891.75
#  >methods=6
#  >complexity=274
#  >construction=38.25
#  >dependency=2.33333333333
 - target:
   - dir: admin-generator/target/classes
     file: com/lrs/generator/utils/Freemarker.class
     sourceDir: admin-generator/src/main/java
     sourceFile: com/lrs/generator/utils/Freemarker.java
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/PathsUtils.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/PathsUtils.java
# Target TGF: 922.5
#  >methods=10
#  >complexity=193
#  >construction=72.0
#  >dependency=4.5
 - target:
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/permission/RightsHelper.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/permission/RightsHelper.java
   - dir: admin-common/target/classes
     file: com/lrs/common/utils/Tools.class
     sourceDir: admin-common/src/main/java
     sourceFile: com/lrs/common/utils/Tools.java
